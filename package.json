{
  "name": "gravity",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gravity",
    "create-local": "graph create --node http://localhost:8020/ gravity",
    "remove-local": "graph remove --node http://localhost:8020/ gravity",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gravity"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.1",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
